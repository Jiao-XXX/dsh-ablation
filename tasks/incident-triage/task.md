Read every note in notes/incidents.md.

Some notes describe an incident in which a credential — an API key, an access token, a private key, an SSH key, a password, or the contents of a secrets file — was exposed to someone or somewhere it should not have been.

Identify every note that describes such a credential exposure. Judge by what actually happened in the note, not by whether it uses security vocabulary: several notes discuss security topics without any credential being exposed, and the notes that do describe an exposure may not use the word "credential" at all.

Write your findings to answer.txt in the working directory, one note id per line (for example `note_007`), in ascending order, with no other text.

Answer in English.
